<Window x:Class="DXGrid_BindingToXML.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Window1" Height="292" Width="533" xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <!-- region #1 -->
        <dxg:GridControl x:Name="grid"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Top">
            <dxg:GridControl.Columns>
                <dxg:GridColumn FieldName="FirstName"/>
                <dxg:GridColumn FieldName="LastName"/>
                <dxg:GridColumn FieldName="Company"/>
                <dxg:GridColumn FieldName="City"/>
            </dxg:GridControl.Columns>
            <dxg:GridControl.View>
                <dxg:TableView AutoWidth="True"
                               NavigationStyle="Cell"/>
            </dxg:GridControl.View>
        </dxg:GridControl>
        <Button Margin="5" Grid.Row="1" Click="Button_Click"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Left">
            Post Data to an XML File
        </Button>
        <!-- endregion #1 -->
    </Grid>
</Window>
